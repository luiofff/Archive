import 'package:flutter/material.dart';

final theme = ThemeData(
    scaffoldBackgroundColor: const Color(0xFF0075FF),
    appBarTheme: const AppBarTheme(
        titleTextStyle: TextStyle(
      fontSize: 20,
    )));
